/**
 * Configuración del frontend SSALFER.
 * Puede sobrescribirse antes del despliegue sin modificar lógica.
 */
window.SSALFER_CONFIG = {
    API_BASE_URL: window.SSALFER_API_URL || "http://localhost:8000/api"
};
